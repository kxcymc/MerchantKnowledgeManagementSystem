# 商家知识管理系统 - 启动指南

本项目包含 **2个前端** 和 **1个后端**，以下是完整的启动步骤。

---

## 📋 前置要求

- **Node.js** (建议 v16 或更高版本)
- **npm** 或 **yarn** 包管理器
- **GraphicsMagick** (后端需要，用于图片处理)

### 安装 GraphicsMagick

**Windows:**
```bash
# 从官网下载安装: http://www.graphicsmagick.org/download.html
```

**macOS:**
```bash
brew install graphicsmagick
```

**Linux:**
```bash
sudo apt-get install graphicsmagick  # Ubuntu/Debian
# 或
sudo yum install GraphicsMagick      # CentOS/RHEL
```

---

## 🚀 启动步骤

### 1️⃣ 后端服务 (暂时废弃的backend)

```bash
# 进入后端目录
cd "暂时废弃的backend"

# 安装依赖
npm install
# 或使用 yarn
yarn

# 启动 ChromaDB 向量数据库
npm run chroma:run
# 或
yarn chroma:run

# 新开一个终端，初始化 ChromaDB
npm run init:chroma
# 或
yarn init:chroma

# 扫描并导入数据
npm run scan
# 或
yarn scan
```

**后端端口**: 需要查看 `index.js` 确认具体端口

---

### 2️⃣ 前端 - QA 聊天机器人 (frontend-QAchatbot)

```bash
# 进入前端 QA 目录
cd frontend-QAchatbot

# 安装依赖
npm install

# 配置 API Key (如果需要)
# 创建 .env.local 文件并设置 GEMINI_API_KEY

# 启动开发服务器
npm run dev
```

**访问地址**: 通常是 `http://localhost:5173` (Vite 默认端口)

---

### 3️⃣ 前端 - 商家知识管理后台 (frontend-toB)

```bash
# 进入前端 toB 目录
cd frontend-toB

# 安装依赖
npm install

# 启动开发服务器
npm run dev
# 或
npm start
```

**访问地址**: 通常是 `http://localhost:3000` 或 `http://localhost:5173`

---

## 📝 完整启动命令（推荐使用多个终端）

### 终端 1 - 后端 ChromaDB
```bash
cd "暂时废弃的backend"
npm install
npm run chroma:run
```

### 终端 2 - 后端初始化（等 ChromaDB 启动后）
```bash
cd "暂时废弃的backend"
npm run init:chroma
npm run scan
```

### 终端 3 - 前端 QA 聊天机器人
```bash
cd frontend-QAchatbot
npm install
npm run dev
```

### 终端 4 - 前端管理后台
```bash
cd frontend-toB
npm install
npm run dev
```

---

## 🔧 常见问题

### 1. 端口冲突
如果端口被占用，可以修改各项目的配置文件：
- Vite 项目: 修改 `vite.config.ts` 中的 `server.port`

### 2. 依赖安装失败
```bash
# 清除缓存后重试
npm cache clean --force
npm install
```

### 3. GraphicsMagick 未安装
后端会报错，请按照上面的说明安装 GraphicsMagick

---

## 📦 项目结构

```
MerchantKnowledgeManagementSystem/
├── 暂时废弃的backend/          # 后端服务（Node.js + Express + ChromaDB）
├── frontend-QAchatbot/          # 前端：问答聊天机器人（React + Vite + Arco Design）
├── frontend-toB/                # 前端：商家知识管理后台（React + Vite + Arco Design Pro）
└── 项目文档《商家知识管理系统和问答机器人》.pdf
```

---

## ✅ 验证启动成功

1. **后端**: ChromaDB 运行在指定端口，控制台无报错
2. **前端 QA**: 浏览器打开对应地址，能看到聊天界面
3. **前端 toB**: 浏览器打开对应地址，能看到管理后台界面

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. Node.js 版本是否符合要求
2. 所有依赖是否正确安装
3. GraphicsMagick 是否已安装
4. 端口是否被占用
